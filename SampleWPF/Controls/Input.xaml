<UserControl x:Class="ChatUI.Controls.Input"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="Input">
            <Setter Property="BorderThickness" Value="0"></Setter>
        </Style>
    </UserControl.Resources>
    <Border Grid.Column="0" Grid.Row="1" CornerRadius="15"  BorderThickness="1" BorderBrush="#aeaeae" Padding="5" Margin="5" Background="#ffffff">
        <TextBox DockPanel.Dock="Left" Style="{StaticResource Input}" Name="textInput" KeyUp="TextInput_KeyUp"/>
    </Border>
</UserControl>
